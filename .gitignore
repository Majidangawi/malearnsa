.env
CLAUDE.local.md
.claude/settings.local.json
node_modules/
.gstack/
.superpowers/

# Apps Script source files contain live API tokens (Tamara Bearer JWT,
# Daftra API key, PayPal Live Secret, Anthropic). NEVER commit to the
# public malearnsa repo. Source of truth = Google Apps Script (deployed
# via clasp from ~/code/.clasp-token-validator/). The paths below are
# local-only working mirrors.
projects/ma-learn-launch/apps-script/token-validator/
projects/ma-learn-launch/tamara-apps-script.js

# Local Apps Script backups (timestamped Code.js snapshots). Useful for
# rollback but no need to ship to the public repo.
archives/apps-script-backups/

# macOS Finder metadata
.DS_Store
**/.DS_Store

# CIW student gallery raw exports — kept locally for re-export.
# The deployed site uses optimized variants (student-N-thumb.jpg
# + student-N-full.jpg) which ARE committed.
creative-ai-workshop/images/Student Work/
creative-ai-workshop/images/_src_originals/
